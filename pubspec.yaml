name: smart_care
description: Care Package - elderly resident monitoring for UK care homes.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # State management
  provider: ^6.1.2

  # Networking
  http: ^1.2.1

  # Local storage
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^10.3.1
  sqflite: ^2.3.3
  path: ^1.9.0

  # Auth / JWT
  jwt_decoder: ^2.0.1

  # Location & maps (Leaflet served via flutter_map / OpenStreetMap)
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  geolocator: ^14.0.2

  # Notifications (local; FCM optional, no Firebase by default)
  flutter_local_notifications: ^22.0.1

  # Charts
  fl_chart: ^1.2.0

  # PDF (incident / care reports)
  pdf: ^3.11.0
  printing: ^5.13.1

  # On-device AI
  flutter_gemma: ^0.13.0
  crypto: ^3.0.3
  path_provider: ^2.1.0

  # Utilities
  intl: ^0.20.2
  uuid: ^4.4.0
  image_picker: ^1.1.2
  permission_handler: ^12.0.2

dependency_overrides:
  # 10.2.0 added built-in Kotlin support (no longer self-applies the Kotlin plugin).
  # Required for AGP 9.0.1 / Kotlin 2.3.20 / Gradle 9.1.0.
  package_info_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
  macos:
    generate: true
    image_path: "assets/images/logo.png"
  web:
    generate: true
    image_path: "assets/images/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
